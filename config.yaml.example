# Meeting Recorder Configuration (Example)
# Copy this file to config.yaml and customize for your setup

# Output directory settings
output_dir:
  base_path: "/home/username/Documents/Obsidian Vault"  # Path to your Obsidian vault
  meetings_subdir: "meetings"  # Subdirectory within base_path for meeting notes

# Audio settings
audio:
  sample_rate: 16000  # Whisper works best with 16kHz
  format: s16le       # 16-bit PCM signed little-endian
  channels: 1         # Mono (mic + speakers mixed)

# Transcription settings
whisper:
  model: base       # Options: tiny, base, small, medium, large
  device: auto      # Options: cpu, cuda, auto (auto detects GPU, falls back to CPU)
  compute_type: default  # Options: default, int8, int8_float16, float16
                         # - int8: fastest on CPU, good quality
                         # - float16: fastest on GPU (CUDA only)
                         # - default: automatic selection based on device
  language: en      # Language code (en, es, fr, etc.)

# GPU Support Notes:
# - NVIDIA GPUs: device=auto will use CUDA automatically
# - AMD GPUs: Limited support - integrated GPUs (like 780M) not supported by ROCm
#   For AMD, use device=cpu with compute_type=int8 for best CPU performance
# - CPU only: Use device=cpu with compute_type=int8 for 2-3x speedup

# Summarization settings
summarization:
  ollama_endpoint: "https://ollama.firecorn.net"  # Ollama server endpoint (HTTPS, no port)
  model: "qwen2.5:14b"  # Recommended for 12GB GPU (RTX 2000 ADA)
  # Alternative models:
  # model: "llama3.1:8b"    # Faster, still good quality
  # model: "qwen2.5:7b"     # Good balance
  # model: "mistral:7b"     # Maximum speed
  # model: "llama3.2:3b"    # Lightweight for low-end hardware

# Output settings
output:
  keep_audio: true  # Keep audio files after transcription
  timestamp_format: "%Y-%m-%d_%H-%M-%S"  # Format for file timestamps

# Version 0.2 settings (planned)
# Uncomment when v0.2 is released:
# output:
#   audio_format: "opus"  # Options: wav, opus, mp3
#   opus_bitrate: 16      # kbps (12-24 range for speech)
#   mp3_bitrate: 64       # kbps (if mp3 selected)
#   delete_wav_after_conversion: true  # Keep only compressed version
